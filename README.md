········································This project provides an automated initialization script and installation package for setting up an Oracle 12c RAC cluster on CentOS 7···················································
····································································Contents include:························································· ··················································································         

This project is:
  A plug-and-play, one-stop solution for configuring and optimizing the initial setup of Oracle 12c R2 12.2.0.1 on a CentOS 7.2009 system

Project Features:
The maximum number of nodes in this project is 9, and the minimum is 1.
When using this project, the 20 IP addresses immediately following Node 1’s public IP must remain unassigned; please plan your IP addresses accordingly.
The IP address immediately preceding Node 1’s public IP is the scan IP and must remain unassigned.
This project is ready for use immediately after system installation. The configuration sequence for Node 1 is: gen_ip.sh, gen_host.sh, orain/rpass, orain/orain_mian.sh.
  The configuration sequence for nodes other than Node 1 is: gen_ip.sh, orain/rpass, orain/orain_mian.sh.
    If network interfaces and IP addresses have already been configured, gen_ip.sh must not be used on any node.

    

gen_ip.sh:
          Automatically generates a configuration file for a static IP address on the host's network interface
gen_host.sh:
          Adds entries to the /etc/hosts file based on the IP address of Node 1 and the number of nodes (entered by the user)
          Enter ORACLE_SID, GRID_SID, and ORACLE_UNQNAME; these will be synchronized across all nodes, and a .bash_profile file will be generated based on the hostname
orain/conf_dir.sh:
          Automates the creation of the installation directory
orain/conf_srvr.sh:
          Automated shutdown of Oracle-recommended services (including disabling the firewall and SELinux)
orain/conf_sys.sh:
          Modifies system configurations based on Oracle’s recommended values (including: disabling transparent large pages, and calculating and optimizing parameters in /etc/sysctl.conf and /etc/security/limits.conf)
orain/gen_sysc.sh:
          Fetch the /etc/hosts file from Node 1 to this node and overwrite the existing one
          Important Note: Before execution, you must configure Node 1’s IP address and the root password in the rpass file
orain/gen_user.sh
           Create the oracle and grid users
           and configure the .bash_profile for both users based on the content generated by gen_host.sh and the /etc/hosts file fetched by orain/gen_sysc.sh, combined with the local network interface IP   
           and grant permissions for the group to create directories 
orain/orain_mian.sh
           Master node initialization and installation orchestration script
orain/rpass
           Requires configuration of Node 1's IP address and root password
                           For example:
                                      192.168.1.1
                                      Passw0rd
orain/yum_conf.sh
           Configures network YUM repositories; since official support for CentOS 7 has been discontinued, Alibaba's YUM repositories are used


Coming soon:
Upgrade and install the Linux 5.4 kernel
Automated silent installation of Grid
Automated silent installation of database software
